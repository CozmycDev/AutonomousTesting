from typing import Optional
import json
import os

class File:
    def __init__(self, name: str, content: str):
        self._name = name
        self._content = content

    @classmethod
    def from_json(cls, filename: str) -> 'File':
        if not os.path.exists(filename):
            raise FileNotFoundError(f"File {filename} does not exist")
        with open(filename, 'r') as file:
            data = json.load(file)
            return cls(name=data['name'], content=data.get('content', ''))

    @classmethod
    def from_text_file(cls, filename: str) -> 'File':
        if not os.path.exists(filename):
            raise FileNotFoundError(f"File {filename} does not exist")
        try:
            with open(filename, 'r') as file:
                file_content = file.read()
            return cls(name=filename, content=file_content)

        except Exception as e:
            raise ValueError(f"Failed to load file {filename}: {str(e)}")

    @classmethod
    def from_json_file(cls, filename: str) -> 'File':
        try:
            with open(filename, 'r') as file:
                data = json.load(file)
                return cls(name=data['name'], content=data.get('content', ''))

        except FileNotFoundError:
            raise ValueError(f"Failed to load JSON file {filename}")
        except json.JSONDecodeError:
            raise ValueError(f"Failed to parse JSON file {filename}")

    @classmethod
    def from_file(cls, filename: str) -> 'File':
        try:
            with open(filename, 'r') as file:
                file_content = file.read()
                return cls(name=filename, content=file_content)

        except FileNotFoundError:
            raise ValueError(f"Failed to load file {filename}")

    @classmethod
    def from_data(cls, data: dict) -> 'File':
        if not data.get('name'):
            raise ValueError("Data is missing name")
        filename = data['name']
        file_content = data.get('content', '')
        return cls(filename=filename, content=file_content)

    def save_to_json(self, filename: str):
        data = {'name': self._name, 'content': self._content}
        with open(filename, 'w') as file:
            json.dump(data, file, indent=4)

    def __str__(self) -> str:
        return f"{self._name}: {self._content}"